/** Minimal shell while RSC loads — client Query cache shows content immediately (A2). */
export function TabLoadingPlaceholder() {
  return <div className="min-h-[40svh] bg-background" aria-hidden />;
}
